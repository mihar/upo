%h1= @news.title

%h2.attachment_count
  = "Novica ima ", stevilo_priponk(@news)
  - admin_section do
    = link_to 'Uredi priponke', admin_link('news_file', {:action => 'edit'})

%h2 Urejanje novice

- form_tag( {:action => 'update', :id => @news}, {:multipart => true, :class => 'nice'} ) do
  = render :partial => 'form'


  = nice_submit_tag


%p.admin_controls
  = link_to 'Uredi priponke', admin_link('news_file', {:action => 'edit'})
  |
  = link_to 'Dodaj datoteko', admin_link('news_file', {:action => 'new_file', :id => @news})
  |
  = link_to 'Dodaj sliko', admin_link('news_file', {:action => 'new_image', :id => @news})
  |
  = link_to 'Izbriši novico', admin_link('news', {:action => 'destroy', :id => @news}), :confirm => del_conf_msg(@news.title)
  |
  = link_to_back normal_news_link
