%h1 Urejanje dokumenta


- form_tag({:action => 'update', :id => @doc}, {:multipart => true, :class => 'nice'}) do
  = render :partial => 'form'
  
  - unless @doc.file.nil?
    %p.file= link_to File.basename(url_for_file_column(@doc, 'file')), url_for_file_column(@doc, 'file')
  
  = nice_submit_tag
  

%p.admin_controls
  = link_to_back zdus_docs_path
  |
  = link_to "Izbriši", admin_zdus_docs_path('destroy', @doc), :confirm => del_conf_msg(@doc.title)
