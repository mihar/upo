%h1 Nova oseba
%h2= "Organ: ", @department.title

- form_tag( {:action => 'people_create'}, {:multipart => true, :class => 'nice'} ) do
  = render :partial => 'people_form'
  = nice_submit_tag


%p.admin_controls= link_to_back normal_link('departments')
