%h1 Urejanje albuma
%h2= @album.title

= render :partial => 'album_form'

%h2 Brisanje albuma

- form_tag({:action => 'delete_album', :id => @album}, :method => 'post', :class => 'nice') do
  = nice_submit_tag "Izbriši album &quot;#{@album.title}&quot;"
  
  
%p.admin_controls= link_to_back normal_link('album', {:id => @album})