%h1 Urejanje povezave

- form_tag( {:action => 'update', :id => @link}, {:class => 'nice'} ) do
  = render :partial => 'form'
  = nice_submit_tag

%p.admin_controls
  = link_to "Izbriši povezavo", admin_link("links", {:action => 'destroy', :id => @link}), :confirm => del_conf_msg(@link.title)
  |
  = link_to_back normal_link('links')
